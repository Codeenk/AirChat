name: native_bridge
description: AirChat native bridge — wake guard FGS + wire keeper service.
version: 1.0.0

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.airchat.native_bridge
        pluginClass: NativeBridgePlugin
